# Requirements for Robust Memory System
# Install with: pip install -r requirements.txt

# Core dependencies
chromadb>=0.4.15
sentence-transformers>=2.2.2
fastmcp>=3.1.1
tiktoken>=0.5.0

# HTTP transport support
uvicorn>=0.30.0
sse-starlette>=1.6.0

# Optional but recommended for better performance
numpy>=1.24.0
torch>=2.0.0  # For sentence-transformers (CPU version is fine)

# Optional: pgvector backend (for --vector-backend pgvector)
psycopg[binary]>=3.1.0
pgvector>=0.3.0

# Optional: 3D vector visualizer (vector_visualizer.py)
plotly>=5.18.0
dash>=2.14.0
scikit-learn>=1.3.0
# umap-learn>=0.5.0  # Optional: for UMAP reduction method

# Optional: TensorBoard Embedding Projector (tensorboard_visualizer.py)
tensorboard>=2.14.0

# Optional: system tray app (tray_app.py)
pystray>=0.19.0
Pillow>=10.0.0

# Development/debugging (optional)
pytest>=7.0.0
black>=23.0.0